/*#include <bits/stdc++.h>*/
#include <iostream>

using namespace std;

#define rep(i, k, n) for (int i = k; i < (n); i++)
#define repr(i, k, n) for (int i = k; i >= (n); i--)

int main() {
  cout << "Hello, World!" << endl;
  return 0;
}
